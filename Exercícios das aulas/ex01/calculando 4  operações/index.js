let n1 = Number(window.prompt('Digite um número'))
let n2 = Number(window.prompt('Digite outro número'))

let soma = n1 + n2
let subtr = n1 - n2
let multipl = n1 * n2
let div = n1 / n2

window.alert(`Resultados: soma ${soma}, Subtração ${subtr}, multiplicação ${multipl}, divisão ${div}`)
